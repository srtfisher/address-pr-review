# Agent Instructions

- Every user-facing change needs a `CHANGELOG.md` entry. Add it under a `## Unreleased` heading at the top (create the heading if it's missing); the release commit renames that heading to the version number.
